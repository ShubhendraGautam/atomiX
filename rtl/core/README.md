# rtl/core/ — CPU architecture entry point

CPU implementations live in the component catalog, not in this directory.
The reference implementation is
[`components/core/pipeline5/`](../../components/core/pipeline5/); select it as
`core.pipeline5` in a configuration. Alternative CPU implementations live in
their own `components/core/<implementation>/` directory or in an external
manifest folder.
